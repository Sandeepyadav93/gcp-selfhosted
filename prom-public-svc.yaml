apiVersion: v1
kind: Service
metadata:
  name: prometheus-public
  namespace: monitoring
spec:
  type: LoadBalancer
  selector:
    prometheus: gmp-collector
  ports:
    - name: http-web
      protocol: TCP
      port: 9090
      targetPort: 9090
